# Ticket System API

This is a backend service for a ticket system built in Golang, fulfilling the Backend Intern Assignment requirements. It uses an in-memory data store for simplicity and JWT for authentication.

## Local Run Instructions

To run the application locally using standard Go tools:

```bash
go mod tidy
go run main.go